import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Fund Flow (NVDR)' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
