import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'SEPA Trend Template' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
