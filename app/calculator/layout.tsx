import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'เครื่องคำนวณ Warrant' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
